services:

  backend:
    build:
      context: ./backend
    ports:
      - "8090:8090"

  frontend:
    build:
      context: ./frontend
    ports:
      - "4200:4200"
    depends_on:
      - backend